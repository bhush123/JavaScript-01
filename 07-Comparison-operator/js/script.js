// comparison operator
// The Comparison operators are mainly used to perform the logical operations that determine the equality or difference between the values.
// comparison operator returns boolean values true or false.

const a = 20;       // number datatype
const b = '30';    //string datatype
 

console.warn(a < b);
console.warn(a > b);
console.warn(a <= b);
console.warn(a >= b);
console.warn(a == b);
console.warn(a != b);
console.warn(a !== b);
console.warn( a === b)



// Q. Differnce between == and ===  ? 

// Ans- In double eaual to we compares only the values not the datatype.
// but in the case of tripple equal to we compares both values as well as datatype. 