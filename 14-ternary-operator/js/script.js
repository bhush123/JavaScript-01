// ternary oprator - a ternary operator is an operator that takes three arguments (or operands) and it is short way to write if else condition in a single statement.

// syntax:
//  condition ? exp1 : exp2 ;

// input from user
const age =Number(prompt('Enter the age'));
console.log(`${(age > 20) ? 'you are adult' : 'your are young'}`);



// if input is given
// const age = 22;
// age > 20 ? console.log('you are adult') : console.log('you are young');